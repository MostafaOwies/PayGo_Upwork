package com.core.utils.constants

object Network {

    const val NAMED_CACHE_DIR = "cache_dir"
    const val REQUEST_TIMEOUT_MILLISEC = (720 * 1000).toLong()
    const val HTTP = "http"
    const val HTTP_CACHE = 10 * 1024 * 1024 // 10 MiB
    const val BASE_URL = "http://api.eduhubegy.com/api/"
}